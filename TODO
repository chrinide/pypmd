* Be careful with units
* Change all formats to 0 -> %. equivalent to C
* If ouput put header for each lib or something
* Wrapp dgemm for matmul
* Wrapp blas dot for dot_product
* Check the cuttz and options
* Add fields like rho, grad
* Add Lowdin S^1/2
* When using gradmod for normalized grad remember to sum small,
  espcially useful for surface, the normal coordinate converges
  faster in the ODE solver. 
* The new error estimation in ODE need abs(yerr) in the steepers
